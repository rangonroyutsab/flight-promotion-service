services:
  web:
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=true

  kibana:
    ports:
      - "5601:5601"

  minio:
    ports:
      - "9000:9000"
      - "9001:9001"
